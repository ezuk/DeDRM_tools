[pytest]
# Obok_plugin/ is an importable package whose __init__.py needs the calibre
# runtime, so tests live at the repo root (outside any package) and import the
# pure title_match module directly via sys.path.
testpaths = tests
addopts = --import-mode=importlib
